name: gt
description: base flutter app
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  dio: 4.0.4
  # 设备信息
  device_info: ^2.0.3
  package_info: ^2.0.2
  # 对象存储
  shared_preferences: ^2.0.15
  # 下拉刷新
  pull_to_refresh_flutter3: ^2.0.1
  # 网络监听
  connectivity_plus: ^2.3.0
  # 事件派发
  event_bus: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

